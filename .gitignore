# 依赖
node_modules/

# 敏感配置（含 API Key）
server/models.json

# 运行时数据
server/logs/
server/state.json
server/token-stats.json
server/benchmark-questions.json
server/benchmark-runs.json
server/fallback.json
server/rules.json
server/server-config.json

# 工具本地数据
.codebuddy/

# lockfile
pnpm-lock.yaml
app/package-lock.json

# 构建产物
app/release/
app/server/
app/renderer/dist/
app/dist-electron/

# 构建脚本重命名的旧依赖目录（删除失败时的残留）
.build-node_modules.old.*

# IDE
.vscode/
.idea/

# 系统文件
.DS_Store
Thumbs.db
BLOG.md
