# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-09-07

### 新增

- **模型测分（Benchmark）**：内置 40 题题库（代码生成、代码修复、SQL、数学、逻辑推理、翻译、指令遵循、结构化输出、长上下文 9 个维度，满分 200 分），支持多模型并发评测
  - 客观题按规则自动判分：`exact` 精确匹配 / `contains` 关键词 / `regex` 正则 / `json` Schema 校验
  - 主观题可指定裁判模型按评分细则（rubric）打分
  - 题库可视化增删改、导入/导出 JSON、一键恢复内置题库
  - 评测结果输出总分与得分率、分类得分率、平均延迟、Token 消耗横向对比
- **Provider 多模型**：一个 Provider 可配置多个模型，每个模型可单独设置最大上下文与最大输出；老配置顶层 `model` 字段自动兼容
- **Provider 管理页改版**：由卡片网格改为一行一个的列表布局，信息层级压缩，模型 chip 化展示并可折叠
- **模型引用体系**：切换与路由的最小单位改为 `Provider名/模型ID`，侧边栏、状态面板、托盘菜单、路由规则、兜底模型四处统一
- **统一设计体系**：抽离 CSS 设计令牌（品牌色/语义色/文本色/边框/圆角/字号），全部页面样式统一维护
- **使用教程页**：接入地址随实际端口动态展示，功能概览补齐 Token 统计与模型测分

### 修复

- 托盘菜单：右键弹出前异步刷新，模型列表与选中态保持最新；修复打包后路径错误导致主进程崩溃
- 评测完成状态值前后端不一致（`done` → `completed`），历史列表不再出现「进行中」卡死记录；服务重启后残留的进行中记录自动标记为失败
- 流式响应：增加行缓冲，TCP 拆包导致 SSE 帧被截断时不再丢失用量统计
- 端点 URL 拼接：自动去除用户配置的尾斜杠，避免拼出 `//v1/messages` 双斜杠
- 自定义规则关键词：英文关键词按单词边界匹配，避免 `go` 误命中 `google`
- API 安全：CORS 收紧为仅放行本机来源；`/api/state`、`/api/rules`、`/api/server-config` 增加入参校验，非法引用/端口/规则不再落盘
- 移除永不命中的 `chatbot_task` 路由条件选项（与引擎实际支持的条件对齐）

## [1.0.0] - 2026-08-xx

### 新增

- 初始版本：Anthropic / OpenAI 双协议请求代理、模型切换、智能路由（auto 模式）、Fallback 兜底、日志系统、Token 统计、Electron 可视化客户端、系统托盘
